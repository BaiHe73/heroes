// 导入vue-router模块
import VueRouter from 'vue-router';
import Vue from 'vue';

// 注册vue-router插件
Vue.use(VueRouter);

// 创建路由对象
var router = new VueRouter({
  // 路由规则
  routes: [

  ]
});

// 模块中导出路由对象
export default router;
